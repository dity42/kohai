[project]
name = "kohai"
version = "0.19.0"
description = "CLI tool for searching, picking, and watching anime via animego and kodik."
readme = "README.md"
authors = [
    { name = "dity", email = "275731998+dity42@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anime-parsers-ru>=1.18.0",
    "platformdirs>=4.11.12",
    "pyfzf>=0.3.1",
    "rich>=15.0.0",
]

[project.scripts]
kohai = "kohai.main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
